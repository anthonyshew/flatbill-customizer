import React from 'react'
import './_Header.scss'

const Header = ({ ...props }) => {
    return (
        <div className="header">
            Make something great with the Header component here!
    </div>
    )
}

export default Header